{
  "name": "pages",
  "version": "1.0.0",
  "description": "content for static site hosting, probably powered by cloudflare pages",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.22.1"
  }
}
